#! /usr/bin/env bash

secret_location=ref+op://k8s/1password-connect--credentials-file--argocd-dev
namespace="1password"

kubectl create namespace $namespace

kubectl create secret generic op-credentials \
  --namespace $namespace \
  --from-literal=1password-credentials.json="$(vals get "$secret_location/contents")"
