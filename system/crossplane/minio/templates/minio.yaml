# TODO: need to sync-wave. this should install prior to below resources
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-minio
spec:
  package: xpkg.upbound.io/vshn/provider-minio:v0.4.3
