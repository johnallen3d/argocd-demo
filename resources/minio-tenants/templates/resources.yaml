apiVersion: minio.min.io/v2
kind: Tenant
metadata:
  name: minio-tenant-0
  namespace: minio-tenant-0
spec:
  pools:
    - name: pool-0
      servers: 1
      volumesPerServer: 4
      volumeClaimTemplate:
        metadata:
          name: data
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 1Gi
